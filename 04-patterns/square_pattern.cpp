#include <bits/stdc++.h>
using namespace std;
int main(){
ios_base::sync_with_stdio(false);
cin.tie(nullptr);
    int n=4;
    for (int i = 0; i < n; i++)
    {  
        for (int  j = 1; j <=n; j++)
        {
            cout<<"* ";
        }
        cout <<endl;
    }
    
    return 0;
}