#include <bits/stdc++.h>
using namespace std;
int main(){
ios_base::sync_with_stdio(false);
cin.tie(nullptr);
    int a=5;
    for (int  i = 0; i < a; i++)
    {
        for (int j = 0; j < i+1; j++)
        {
            cout<<"* ";

        }
        cout<<endl;
    }
    
    return 0;
}