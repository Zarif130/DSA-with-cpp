#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    // c++ a array er nam e akta pointer hoy.!
    // jta arr[0] tomo index k point kore..!
    int arr[] = {1, 2, 3, 4};
    cout << *arr << endl;

    // array pointer is also called a contstant pointer...!
    
    return 0;
}