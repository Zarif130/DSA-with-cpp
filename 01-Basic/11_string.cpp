#include <bits/stdc++.h>
using namespace std;
int main(){
ios_base::sync_with_stdio(false);
cin.tie(nullptr);
    string name="zarif";
    cout<<"my name is "<<name<<endl;
    cout<<"so the length of name is "<<name.length()<<endl;
    cout<<"so the substring of name is "<<name.substr(0,3)<<endl;
    // cout<<"my name is "<<name;
    return 0;
}