#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    double price = 100.49;
    int newprice = (int)price;

    cout << newprice;

    return 0;
}